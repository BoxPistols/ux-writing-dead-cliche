# DESIGN

dead-cliche の Web 面 (アプリ・提案フォーム・今後のページ) のブランディングと
デザインルールです。実装は docs/design-tokens.css を単一の情報源とし、
このファイルはその判断理由を持ちます。

## ブランドの前提

このプロダクトは、文章から装飾の癖を取り除く道具です。したがって UI 自身が
装飾の癖を持った時点でブランドが破綻します。判断に迷ったら「この要素は情報を
足しているか」で決めます。足していない要素は消します。

声のトーンは文章側の規律と同一です。敬体、端的、誇張なし。UI 文言は自前の
ux-microcopy 辞書に従います (ボタンは動作名詞、エラーは事象と対処、絵文字なし)。

## 名前とマーク

- 表記は小文字の `dead-cliche`。見出しでは IBM Plex Mono の太字で組み、
  これをロゴタイプとして扱います
- シンボルマークは当面作りません。必要になったら、寸法を手で決めず幾何拘束から
  導く方法で作ります (2026-08-22 のロゴ却下の教訓)

## タイポグラフィ

| 役割 | 書体 | 補足 |
| --- | --- | --- |
| UI・本文 | IBM Plex Sans JP | 400 / 500 / 700 |
| 検出表現・コード・ロゴタイプ | IBM Plex Mono | 検出対象の表現は必ずこの書体で示す |

- 明朝体 (セリフ) は使いません (2026-08-30 指示)。フォールバックは
  "Hiragino Kaku Gothic ProN", "Noto Sans JP", sans-serif
- 文字サイズの最小は 12px。例外はありません。段階は 12 / 13 / 14.5 / 16 / 18 / 22
- 日本語の本文には `word-break: auto-phrase` と `line-break: strict` を当て、
  行長はコンテナ幅に任せます。`ch` 単位での幅上限を本文に使いません (2026-08-23 決定)

## 色

基調は藍 (印刷と校正の世界の色)。意味色はアクセントと分離し、severity の表示にだけ使います。

| トークン | light | dark | 役割 |
| --- | --- | --- | --- |
| paper | #F6F6F3 | #15181B | ページの地 |
| card | #FFFFFF | #1D2125 | カード・入力欄の面 |
| ink | #22262A | #E7E6E1 | 本文 |
| ink-soft | #545C63 | #A2A9AF | 補足・説明 |
| line | #DDDBD3 | #333A40 | 罫線 |
| indigo | #2A4B7C | #8FB0DE | アクセント。リンク・主ボタン・選択状態 |
| indigo-soft | #E9EEF5 | #253141 | アクセントの淡い面 |
| shu | #B8432C | #E07A5F | error の意味色 |
| amber | #8A6A1F | #D3B265 | warn の意味色 |
| green | #2E6E4E | #7FBD9A | 成功・完了の意味色 |
| mono-bg | #EFEEE8 | #121517 | コード・検出表現の地 |

- 文字と地の組み合わせはすべてコントラスト比 4.5:1 以上 (実測の最小値は 5.05)。
  トークンを変えたら再計算します
- 避ける色: 紫から青へのグラデーション、蛍光グリーン、クリーム地にテラコッタ、
  色相のない純グレー。いずれも生成 AI の既定値に収束するためです

## 面と区切り

- 区切りは 1px の罫線と面の色で作ります。影は使いません
- 1 辺だけの太い帯・ボーダー (カード上辺の色帯、左辺 2px 以上の強調線など) は
  禁止です (2026-08-21 却下。種別は札と文字色で既に示せています)
- 角丸は 4〜6px。丸めるほど既定値に寄るため、これ以上は使いません

## コンポーネント

- ボタン: 主ボタンは indigo の面に paper の文字。動作名詞のラベル
- 札 (タグ・チップ): 1px 罫線の小さな面。severity は札の文字色でのみ示す
- 入力欄: card の面 + 1px 罫線。フォーカスは 2px の outline (indigo)
- 「代わりに書くこと」のような引用的な情報は、線ではなく indigo-soft の面で示す
- オーバーレイ・ポップアップを作る場合は、不透明度 80〜90% の半透明 +
  backdrop-filter: blur() のグラスモーフィズムにする (利用者指定の絶対ルール)

## モーション

動きは 1 箇所に集約します。現在はコピー完了のフィードバック (ボタンの色と文言の
変化) だけです。`prefers-reduced-motion` を尊重します。

## 検査 (機械で止める)

CI が docs/ の HTML と CSS を走査します。

- 12px 未満の font-size が存在しないこと
- 2px 以上の 1 辺ボーダー (border-left など) が存在しないこと

機械で判定できない却下事例は、日付と理由をこのファイルに追記します。

## 禁止の一覧 (実際に却下されたもの)

| 対象 | 却下日 | 代わりに |
| --- | --- | --- |
| 明朝体の見出し | 2026-08-30 | IBM Plex Sans JP の太字とサイズで階層を作る |
| 1 辺だけの帯・ボーダー + 角丸 | 2026-08-21 | 札・文字色・面の色 |
| 本文への ch 単位の幅上限 | 2026-08-23 | コンテナ幅 + 文節折り + 禁則 |
| 太字記法の散布 (文章) | 2026-08-10 | 平文で端的に。構造は見出しで |
